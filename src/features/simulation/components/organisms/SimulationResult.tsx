import { FC } from 'react'
import { useTranslation } from 'react-i18next'

export type SimulationResultProps = {}

/**
 * Organism component of optimization simulation results
 * @returns
 */
export const SimulationResult: FC<SimulationResultProps> = () => {
  const { t } = useTranslation()

  // ここでデータ取得
  // urlに基本保存する
  // react-routerで

  return <div>after</div>
}
